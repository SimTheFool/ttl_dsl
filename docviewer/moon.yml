$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  dev:
    command: "cargo tauri dev"
    options:
      persistent: true
  build:
    command: "cargo tauri build"
  bump:
    command: "cargo release version --execute --quiet --no-confirm"
    options:
      cache: false