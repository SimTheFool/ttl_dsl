name: Test branch

on:
  push:
    branches:
      - '**'
      - '!main'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Install
      uses: ./.github/actions/install
      with:
        cache_key: bbb

    - run: 'moon ci :test'