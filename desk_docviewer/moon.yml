$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  dev:
    command: "cargo tauri dev"
    option:
      persistent: true
  build:
    command: "cargo tauri build"
  bump:
    command: "cargo release version --execute"
    options:
      cache: false